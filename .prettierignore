# GENERATED (base) FROM standards/root-files/.prettierignore.
# This is the GENERIC baseline shared by every site. Site-specific ignores
# go BELOW the "# --- site-specific ---" marker, which sync-standards.sh
# preserves. Do not edit the section above the marker by hand.

# Minified & source-map artifacts.
**/*.map
**/*.min.css
**/*.min.js

# Vendored / generated output.
node_modules/**
lighthouse_results/**

# The web-standards submodule is vendored content — never lint it here.
standards/**

# --- site-specific --- (edit freely below this line; kept across syncs)
# Upstream al-folio vendor doc (72 KB). Formats clean locally under the
# pinned prettier@3.8.4 + plugin@1.10.3, but the GitHub runner deterministically
# reports it unformatted (a CI-only Unicode/locale divergence we could not
# reproduce). It's vendor documentation, not authored content — ignore it.
CUSTOMIZE.md
# Generated design-token partial synced from standards/design-tokens; the
# generator is its source of truth (see bin note), not prettier.
_sass/_tokens.scss
assets/css/main.scss
assets/js/distillpub/template.v2.js
assets/js/search/*.js
assets/plotly/demo.html
_posts/2015-10-20-math.md
_sass/font-awesome/*.scss
_scripts/*
# Ignore citation YAML file generated by script
_data/citations.yml
